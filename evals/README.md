# Evals

Lightweight evaluation examples for this Skill.

- `cases.jsonl`: case-to-risk mapping expectations.
- `misuse.jsonl`: misuse prompts and expected safe behavior.

These files are intentionally simple. They are meant for maintainers and contributors to reason about expected behavior when changing `SKILL.md`, `references/`, or `knowledge/`.
